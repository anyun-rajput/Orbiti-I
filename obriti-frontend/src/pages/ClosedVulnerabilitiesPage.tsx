import { Layout } from "@/components/Layout";
import { ClosedVulnerabilities } from "@/components/ClosedVulnerabilities";

export default function ClosedVulnerabilitiesPage() {
  return (
    <Layout>
      <ClosedVulnerabilities />
    </Layout>
  );
}